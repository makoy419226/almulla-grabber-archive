/**
 * Keep unfinished experiences out of public navigation and direct routes.
 * Set this to `true` only after the sectors experience is approved for release.
 */
export const SECTORS_EXPERIENCE_ENABLED = false;
